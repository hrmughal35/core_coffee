import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact & Location – Core Coffee | DHA Phase 8 Lahore",
  description:
    "Find Core Coffee: Phase 8 (X), Park View, 23 CCA, DHA Lahore. Phone, hours, map & directions.",
};

export default function ContactLayout({
  children,
}: { children: React.ReactNode }) {
  return children;
}
